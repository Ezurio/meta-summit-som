[pkcs11_sect]
module = @LIBDIR@/ossl-modules/pkcs11.so
pkcs11-module-path = @LIBDIR@/pkcs11/aws_kms_pkcs11.so
activate = 1

[provider_sect]
aws_kms_pkcs11 = pkcs11_sect

# When activating a custom provider, the default provider must also be activated explicitly.
[default_sect]
activate = 1
