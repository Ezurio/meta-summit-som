#!/bin/sh

case "${1}" in
	start)
		/usr/sbin/summit-prov-ti.sh
		;;

	stop)
		;;

	restart|reload)
		/usr/sbin/summit-prov-ti.sh
		;;

	*)
		echo "Usage: $0 {start|stop|restart}"
		exit 1
esac

exit $?
