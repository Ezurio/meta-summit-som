#!/bin/sh

case "$1" in
start)
    /usr/sbin/mount_data.sh start
    ;;
stop)
    /usr/sbin/mount_data.sh stop
    ;;
restart)
    "$0" stop
    "$0" start
    ;;
*)
    echo "Usage: $0 {start|stop|restart}"
    exit 1
    ;;
esac
